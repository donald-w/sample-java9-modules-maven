box: openjdk:9

build:
steps:
    - script:
        name: dumpjavaversion
        code: |
          java -version
    - script:
        name: setupssl
        code: |
          apt-get install -y ca-certificates-java
    - script:
        name: mvn
        code: |
          chmod +x mvnw; ./mvnw clean install