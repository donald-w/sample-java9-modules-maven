box: openjdk:9

build:
  steps:
    - script:
        name: setupssl
        code: |
          apt-get install -y java-cacerts
    - script:
        name: mvn
        code: |
          chmod +x mvnw; ./mvnw clean install